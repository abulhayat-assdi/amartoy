import { ContactSupportPanel } from "@/components/pages/contact-support-panel";

export default function ContactPage() {
  return (
    <section className="section contact-support-page">
      <div className="container contact-support-page__inner">
        <ContactSupportPanel />
      </div>
    </section>
  );
}
